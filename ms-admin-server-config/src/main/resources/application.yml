server:
  port: 8888
spring:
  application:
    name: ms-admin-server-config
#Configuracion Local
#  profiles:
#    active: native
#  cloud:
#    config:
#      server:
#        native:
#          searchLocations: file:///D:/Galaxy/Proyecto-Github/Nutrition-app/Microservices-project/ms-admin-server-data

#Configuración Cloud (git)
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jcedenon92/Microservices-project.git
          search-paths:
          - ms-admin-server-data
          username: ${USER_NAME}
          password: ${USER_PASSWORD}
          clone-on-start: true
          force-pull: true
          timeout: 5
          default-label: feature/upgradeMicroservices